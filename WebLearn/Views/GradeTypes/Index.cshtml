@model IEnumerable<WebLearn.Models.GradeTypes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GradeTypesName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GradeTypesName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GradeTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GradeTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GradeTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
